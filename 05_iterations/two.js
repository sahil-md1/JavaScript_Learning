let index = 0;

while (index <=10) {
    // console.log(`The value of index will be ${index}`);
    index= index + 2
}


let myArray = ['superman', 'flash', 'ironman', 'batman'];
let arr = 0
while (arr < myArray.length) {
    // console.log(`Value is ${myArray[arr]}`);
    arr = arr +1
}

let score = 0;
do {
    console.log(`score will be ${score}`);
    score++

} while (score <= 10)